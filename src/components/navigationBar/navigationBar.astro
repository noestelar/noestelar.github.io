---
import { getLangFromUrl, useTranslations } from "../../i18n/utils";
import {
  IoLogoLinkedin,
  IoLogoGithub,
  IoLogoTwitter,
  IoMenu,
} from "react-icons/io5/index.js";
import React from "react";
import ThemeToggle from "./themeToggleButton";
import NavigationMenu from "./navigationMenu";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const links = [
  { name: t("nav.home"), href: `/${lang}` },
  { name: t("nav.works"), href: `/${lang}/works` },
  { name: t("nav.uses"), href: `/${lang}/uses` },
];
// const menu = [
//   {
//     name: "Projects",
//     url: "/projects",
//   },
//   {
//     name: "Uses",
//     url: "/uses",
//   },
// ];
---

<header class="">
  <nav
    class="fixed w-full z-10 bg-opacity-35 backdrop-filter backdrop-blur-md font-quicksand p-3"
  >
    <div
      class="flex flex-row items-center md:justify-center font-quicksand justify-between"
    >
      <h1 class="font-portal mr-3"><a href={`/${lang}`}>NOÉ RIVERA</a></h1>
      <div class="hidden sm:flex mx-10">
        <ul class="flex flex-row float-right">
          {
            links.map((menuItem) => (
              <li class="inline-block cursor-pointer hover:underline mx-3">
                <a href={menuItem.href}>{menuItem.name}</a>
              </li>
            ))
          }
        </ul>
      </div>
      <div class="flex flex-row ml-1 items-center">
        <!-- <a
          href="https://www.linkedin.com/in/noealirl/"
          target="_blank"
          class="cursor-pointer"
        >
          <div class="mx-2 hidden md:block" style="font-size: 24px;">
            <IoLogoLinkedin />
          </div>
        </a>
        <a
          href="https://twitter.com/noestelar"
          target="_blank"
          class="cursor-pointer"
        >
          <div class="mx-2 hidden md:block" style="font-size: 24px;">
            <IoLogoTwitter />
          </div>
        </a>
        <a
          href="https://github.com/Noe-ali"
          target="_blank"
          class="cursor-pointer"
        >
          <div class="mx-2 hidden md:block" style="font-size: 24px;">
            <IoLogoGithub />
          </div>
        </a> -->

        <ThemeToggle client:visible />
        <div class="sm:hidden">
          <NavigationMenu client:visible links={links} />
        </div>
      </div>
    </div>
  </nav>
</header>
